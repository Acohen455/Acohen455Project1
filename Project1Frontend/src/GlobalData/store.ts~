export const store = {
    loggedInUser: {
        userId: 0,
        username: "",
        role: "admin",
    },

    getLoggedInUser: function () {
        return this.loggedInUser.role.toUpperCase();
    }
}